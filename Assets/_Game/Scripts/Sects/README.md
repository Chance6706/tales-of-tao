# Sects Module (Phase 3)

Planned systems:
- `SectController` — manages sect state (resources, territory, units, research)
- `SectRegistry` — runtime registry of all active sects
- `SectFactory` — creates sect instances from SectDataSO templates

See GDD v2 Section 4.1 for sect mechanics.
