# Combat Module (Phase 3)

Planned systems:
- `CombatResolver` — deterministic combat calculation (attack vs defense + terrain bonus)
- `CombatCommand` — Command-pattern wrapper for combat actions (supports undo)
- `CasualtyCalculator` — applies losses based on combat outcome and unit tiers

See GDD v2 Section 4.2 for combat formulas.
